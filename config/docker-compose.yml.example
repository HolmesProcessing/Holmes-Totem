officemeta:
  build: ../src/main/scala/org/novetta/zoo/services/officemeta
  ports:
    - "7730:8080"
  restart: unless-stopped
  volumes:
    - /tmp:/tmp:ro

peid:
  build: ../src/main/scala/org/novetta/zoo/services/peid
  ports:
    - "7720:8080"
  restart: unless-stopped
  volumes:
    - /tmp:/tmp:ro

peinfo:
  build: ../src/main/scala/org/novetta/zoo/services/peinfo
  ports:
    - "7705:8080"
  restart: unless-stopped
  volumes:
    - /tmp:/tmp:ro

virustotal:
  build: ../src/main/scala/org/novetta/zoo/services/virustotal
  ports:
    - "7710:8080"
  restart: unless-stopped
  volumes:
   - /tmp:/tmp:ro

yara:
  build: ../src/main/scala/org/novetta/zoo/services/yara
  ports:
    - "7701:8080"
  restart: unless-stopped
  volumes:
   - /tmp:/tmp:ro

zipmeta:
  build: ../src/main/scala/org/novetta/zoo/services/zipmeta
  ports:
    - "7715:8080"
  restart: unless-stopped
  volumes:
   - /tmp:/tmp:ro
